Create a .NET Solution.
Write a complet solution that can be compiled and execute with out additional coding required. 
Create all Classes in separate files. 
Create all Interfaces in separate files. 
Create all Enums in separate files. 
Create all Records in separate files. 

Use Fluent Interfaces if possible.

Write all code for a business analyst or entry level programmer to read with comments for anything complex that they might not understand. 

using C# net8.0 and the C# Roslyn Compiler to create an Abstract Syntax Tree that includes all metadata.
Generate a Abstract Syntax Tree Pretty Printer.
Generate an AST to code Reflow Generator in order to build and execute the generated code using Roslyn
Generate 25 Unit Tests for lexing the Abstract Syntax Tree.

Use the Abstract Syntax Tree Pretty Printer in the Unit Tests to display the Abstract Syntax Tree.
Use the Abstract Syntax Tree Reflow Generator in the Unit Tests to execute the generated code.

Use only the Microsoft's Unit Test framework.
Use only complex code as input in all Unit Tests.
Use only the Microsoft Basic Component Library.
Use only explicit Types.

name all variables with complete nouns using the underlying type's name if possible. 
name all variables with either a prefix or a postfix if the underlying noun needs more clarification.